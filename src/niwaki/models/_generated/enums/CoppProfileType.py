# ruff: noqa
# Generated by niwaki generate_enums — do not edit manually.
from __future__ import annotations

from enum import StrEnum


class CoppProfileType(StrEnum):
    CUSTOM = "custom"
    DEFAULT = "default"
    LENIENT = "lenient"
    MODERATE = "moderate"
    STRICT = "strict"

    @classmethod
    def _missing_(cls, value: object) -> CoppProfileType | None:
        """Accept numeric aliases used in ACI schema (hex or decimal)."""
        _aliases: dict[str, str] = {
            "1": "strict",
            "2": "moderate",
            "3": "lenient",
            "4": "default",
            "5": "custom",
        }
        if isinstance(value, str) and value in _aliases:
            return cls(_aliases[value])
        return None
