# ruff: noqa
# Generated by niwaki generate_enums — do not edit manually.
from __future__ import annotations

from enum import StrEnum


class DbgexpServiceType(StrEnum):
    POLICY = "policy"
    VMM = "vmm"
    BOOTMGR = "bootmgr"
    FAULTS_EVENTS = "faults-events"
    DEBUGGER = "debugger"
    IDMGR = "idmgr"
    TOPOLOGY = "topology"
    STATS = "stats"
    SERVICES = "services"
    SYSTEM = "system"

    @classmethod
    def _missing_(cls, value: object) -> DbgexpServiceType | None:
        """Accept numeric aliases used in ACI schema (hex or decimal)."""
        _aliases: dict[str, str] = {
            "1": "policy",
            "128": "stats",
            "16": "debugger",
            "2": "vmm",
            "256": "services",
            "32": "idmgr",
            "4": "bootmgr",
            "512": "system",
            "64": "topology",
            "8": "faults-events",
        }
        if isinstance(value, str) and value in _aliases:
            return cls(_aliases[value])
        return None
