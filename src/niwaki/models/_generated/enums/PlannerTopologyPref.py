# ruff: noqa
# Generated by niwaki generate_enums — do not edit manually.
from __future__ import annotations

from enum import StrEnum


class PlannerTopologyPref(StrEnum):
    EXISTING = "existing"
    NEW = "new"
    NEW_AND_EXISTING = "new_and_existing"

    @classmethod
    def _missing_(cls, value: object) -> PlannerTopologyPref | None:
        """Accept numeric aliases used in ACI schema (hex or decimal)."""
        _aliases: dict[str, str] = {
            "0": "new",
            "1": "existing",
            "2": "new_and_existing",
        }
        if isinstance(value, str) and value in _aliases:
            return cls(_aliases[value])
        return None
