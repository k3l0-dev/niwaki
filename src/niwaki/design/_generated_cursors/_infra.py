# ruff: noqa
# pyright: reportIncompatibleMethodOverride=false, reportUnnecessaryContains=false
# Generated by niwaki generate_design — do not edit manually.
# Re-generate: uv run python -m niwaki._codegen.generate_design
#
# The pyright/mypy override waivers are deliberate: typed cursors narrow the
# base Cursor signatures (set/bind take the curated kwargs), and same-label
# makers across ancestor mixins resolve by MRO — nearest level wins, exactly
# like the runtime.
#
# Typed cursor classes for the design DSL.  Signatures only — every body
# delegates to the dynamic runtime in niwaki.design._cursor.  Bodies read
# locals() at function entry (arguments only) and prune None values, so a
# parameter reaches the runtime only when the caller provided it.
"""Typed design cursors (generated) — infra domain."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, cast

from niwaki.design._cursor import Cursor, _prune
from niwaki.design._generated_cursors._uni import _UniMakers

if TYPE_CHECKING:
    from niwaki.models._generated.enums.DhcpOwner import DhcpOwner
    from niwaki.models._generated.enums.DhcpRelayVisibility import DhcpRelayVisibility
    from niwaki.models._generated.enums.DppBurstUnit import DppBurstUnit
    from niwaki.models._generated.enums.DppConformRateAction import DppConformRateAction
    from niwaki.models._generated.enums.DppExceedRateAction import DppExceedRateAction
    from niwaki.models._generated.enums.DppMode import DppMode
    from niwaki.models._generated.enums.DppRateUnit import DppRateUnit
    from niwaki.models._generated.enums.DppSharingMode import DppSharingMode
    from niwaki.models._generated.enums.DppType import DppType
    from niwaki.models._generated.enums.DppViolateRateAction import DppViolateRateAction
    from niwaki.models._generated.enums.FvnsBlkAllocMode import FvnsBlkAllocMode
    from niwaki.models._generated.enums.FvnsBlkRole import FvnsBlkRole
    from niwaki.models._generated.enums.InfraLagT import InfraLagT
    from niwaki.models._generated.enums.L1AutoNeg import L1AutoNeg
    from niwaki.models._generated.enums.L1EmiRetrain import L1EmiRetrain
    from niwaki.models._generated.enums.L1FECMode import L1FECMode
    from niwaki.models._generated.enums.L1PktT import L1PktT
    from niwaki.models._generated.enums.L1PortPhyMediaType import L1PortPhyMediaType
    from niwaki.models._generated.enums.L1Speed import L1Speed
    from niwaki.models._generated.enums.L1StormCtrlActionT import L1StormCtrlActionT
    from niwaki.models._generated.enums.L1StormCtrlCfgValid import L1StormCtrlCfgValid
    from niwaki.models._generated.enums.LacpMode import LacpMode
    from niwaki.models._generated.enums.LldpAdminSt import LldpAdminSt
    from niwaki.models._generated.enums.LldpDCBXPVerType import LldpDCBXPVerType
    from niwaki.models._generated.enums.McpMcpMode import McpMcpMode
    from niwaki.models._generated.enums.McpMcpPduPerVlan import McpMcpPduPerVlan
    from niwaki.models._generated.enums.NwIfAdminSt import NwIfAdminSt
    from niwaki.models._generated.enums.PcIfControl import PcIfControl
    from niwaki.models._generated.enums.QosDppPolAdminState import QosDppPolAdminState


class _InfraMakers(Cursor):
    """Makers declared at the infra level (``infraInfra``)."""

    __slots__ = ()

    def dhcp_relay_policy(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        relay_mode: DhcpRelayVisibility | str | None = None,
        display_name: str | None = None,
        owner: DhcpOwner | str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> InfraDhcpRelayPolicyCursor:
        """Declare a ``dhcpRelayP`` child under the infra level.

        The DHCP relay profile, with one or more helper addresses in it, configures a DHCP relay
        agent for forwarding DHCP packets to a remote server.

        Args:
            name: The DHCP relay policy name. This name can be up to 64 alphanumeric characters.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            relay_mode: Represents the mode to be used for doing DHCP relay Values: ``not-
                visible``, ``visible``. Default: ``visible``.
            owner: Represents the target relay servers ownership Values: ``infra``, ``tenant``.
                Default: ``infra``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "InfraDhcpRelayPolicyCursor",
            self._invoke_maker("dhcp_relay_policy", (name,), _prune(params)),
        )

    def dpp_policy(
        self,
        name: str,
        *,
        admin_st: QosDppPolAdminState | str | None = None,
        annotation: str | None = None,
        excessive_burst: str | None = None,
        excessive_burst_unit: DppBurstUnit | str | None = None,
        burst: str | None = None,
        burst_unit: DppBurstUnit | str | None = None,
        confirm_action: DppConformRateAction | str | None = None,
        conform_mark_cos: str | None = None,
        conform_mark_dscp: str | None = None,
        description: str | None = None,
        exceed_action: DppExceedRateAction | str | None = None,
        exceed_mark_cos: str | None = None,
        exceed_mark_dscp: str | None = None,
        bit_or_packet: DppMode | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        peak_rate: str | None = None,
        peak_rate_unit: DppRateUnit | str | None = None,
        rate: str | None = None,
        rate_unit: DppRateUnit | str | None = None,
        policer_sharing_mode: DppSharingMode | str | None = None,
        type: DppType | str | None = None,
        userdom: str | None = None,
        violate_action: DppViolateRateAction | str | None = None,
        violate_mark_cos: str | None = None,
        violate_mark_dscp: str | None = None,
    ) -> InfraDppPolicyCursor:
        """Declare a ``qosDppPol`` child under the infra level.

        Define a Data Plane Policing policy. User is supposed to use this in scenarios where the
        incoming traffic need to be policed to certain levels

        Args:
            name: The name of the object.
            admin_st: The Administrative state of the policy Values: ``disabled``, ``enabled``.
                Default: ``disabled``.
            annotation: User annotation. Suggested format orchestrator:value
            excessive_burst: Excessive burst size (2R3C policer only)
            excessive_burst_unit: Excessive Burst unit - none, Kilo, Mega, Giga, ms, us Values:
                ``giga``, ``kilo``, ``mega``, ``msec``, ``unspecified``, ``usec``. Default:
                ``unspecified``.
            burst: Committed burst size, number of packets to absorb during a burst
            burst_unit: Burst unit - byte, kbyte, mbyte etc. Values: ``giga``, ``kilo``,
                ``mega``, ``msec``, ``unspecified``, ``usec``. Default: ``unspecified``.
            confirm_action: Confirm action Values: ``drop``, ``mark``, ``transmit``. Default:
                ``transmit``.
            conform_mark_cos: Conform Mark cos
            conform_mark_dscp: Conform Mark Dscp
            description: Specifies a description of the policy definition.
            exceed_action: Exceed action Values: ``drop``, ``mark``, ``transmit``. Default:
                ``drop``.
            exceed_mark_cos: Exceed Mark cos
            exceed_mark_dscp: Exceed Mark Dscp
            bit_or_packet: Policer mode - bytes or packet policer Values: ``bit``, ``packet``.
                Default: ``bit``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            peak_rate: Peak rate (pir) (2R3C policer only)
            peak_rate_unit: Peak Rate unit - none, Kilo, Mega, Giga Values: ``giga``, ``kilo``,
                ``mega``, ``unspecified``. Default: ``unspecified``.
            rate: Allowed rate, committed rate at which the packets are allowed into the system
            rate_unit: Rate unit - bps, kbps, mbps, packets etc. Values: ``giga``, ``kilo``,
                ``mega``, ``unspecified``. Default: ``unspecified``.
            policer_sharing_mode: Policer sharing mode Values: ``dedicated``, ``shared``.
                Default: ``dedicated``.
            type: Policer type Values: ``1R2C``, ``2R3C``. Default: ``1R2C``.
            violate_action: Violate action Values: ``drop``, ``mark``, ``transmit``. Default:
                ``drop``.
            violate_mark_cos: Violate Mark cos
            violate_mark_dscp: Violate Mark Dscp
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "InfraDppPolicyCursor",
            self._invoke_maker("dpp_policy", (name,), _prune(params)),
        )

    def cdp_policy(
        self,
        name: str,
        *,
        admin_state: NwIfAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> CdpPolicyCursor:
        """Declare a ``cdpIfPol`` child under the infra level.

        The CDP interface policy, which is primarily used to obtain protocol addresses of
        neighboring devices and discover the platform of those devices. CDP can also be used to
        display information about the interfaces your router uses.

        Args:
            name: The interface policy name.
            admin_state: Admin state Values: ``disabled``, ``enabled``. Default: ``enabled``.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "CdpPolicyCursor",
            self._invoke_maker("cdp_policy", (name,), _prune(params)),
        )

    def lldp_policy(
        self,
        name: str,
        *,
        receive_state: LldpAdminSt | str | None = None,
        transmit_state: LldpAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        dcbxp_version: LldpDCBXPVerType | str | None = None,
        userdom: str | None = None,
    ) -> LldpPolicyCursor:
        """Declare a ``lldpIfPol`` child under the infra level.

        The LLDP interface policy, which defines a common configuration that will apply to one or
        more LLDP interfaces. LLDP uses the logical link control (LLC) services to transmit and
        receive information to and from other LLDP agents.

        Args:
            name: LLDP interface policy name.
            receive_state: Receive admin state Values: ``disabled``, ``enabled``. Default:
                ``enabled``.
            transmit_state: Transmit admin state Values: ``disabled``, ``enabled``. Default:
                ``enabled``.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            dcbxp_version: Port DCBXP Version Values: ``CEE``, ``IEEE``. Default: ``CEE``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LldpPolicyCursor",
            self._invoke_maker("lldp_policy", (name,), _prune(params)),
        )

    def lacp_policy(
        self,
        name: str,
        *,
        annotation: str | None = None,
        control: PcIfControl | str | None = None,
        description: str | None = None,
        maximum_number_of_links: int | None = None,
        minimum_number_of_links: int | None = None,
        mode: LacpMode | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LacpPolicyCursor:
        """Declare a ``lacpLagPol`` child under the infra level.

        The PortChannel policy enables you to bundle several physical ports together to form a
        single port channel. LACP enables a node to negotiate an automatic bundling of links by
        sending LACP packets to the peer node.

        Args:
            name: Specifies the policy name.
            annotation: User annotation. Suggested format orchestrator:value
            control: LAG control properties Values: ``fast-sel-hot-stdby``, ``graceful-conv``,
                ``load-defer``, ``susp-individual``, ``symmetric-hash``. Default: ``fast-sel-
                hot-stdby``.
            description: Specifies a description of the policy definition.
            maximum_number_of_links: maximum links Default: ``16``.
            minimum_number_of_links: minimum links @@@ MinLinks in the port channel Default:
                ``1``.
            mode: mode Values: ``active``, ``explicit-failover``, ``mac-pin``, ``mac-pin-
                nicload``, ``off``, ``passive``. Default: ``off``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LacpPolicyCursor",
            self._invoke_maker("lacp_policy", (name,), _prune(params)),
        )

    def link_level_policy(
        self,
        name: str,
        *,
        annotation: str | None = None,
        auto_negotiation_on_off: L1AutoNeg | str | None = None,
        description: str | None = None,
        dfe_delay_ms: str | None = None,
        enable_disable_emi_retrain: L1EmiRetrain | str | None = None,
        fec_mode: L1FECMode | str | None = None,
        link_debounce_interval_msec: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        physical_media_type: L1PortPhyMediaType | str | None = None,
        speed: L1Speed | str | None = None,
        userdom: str | None = None,
    ) -> LinkLevelPolicyCursor:
        """Declare a ``fabricHIfPol`` child under the infra level.

        The host interface policy specifies the layer 1 parameters of host facing ports.

        Args:
            name: The name of the interface policy. This name can be up to 64 characters. Note
                that you cannot change this name after the object has been saved.
            annotation: User annotation. Suggested format orchestrator:value
            auto_negotiation_on_off: The policy auto-negotiation. Auto-negotiation is an
                optional function of the IEEE 802.3u Fast Ethernet standard that enables devices
                to automatically exchange information over a link about speed and duplex
                abilities. Values: ``off``, ``on``, ``on-enforce``. Default: ``on``.
            description: Specifies a description of the policy definition.
            enable_disable_emi_retrain: Values: ``disable``, ``enable``. Default: ``disable``.
            fec_mode: Forwarding error correction (FEC) mode. By incorporating error correction
                information into data packets, FEC improves the reliability of data transmission
                over networks that lack guaranteed delivery mechanisms. Values: ``auto-fec``,
                ``cl74-fc-fec``, ``cl91-rs-fec``, ``cons16-rs-fec``, ``disable-fec``, ``ieee-rs-
                fec``, ``inherit``, ``kp-fec``. Default: ``inherit``.
            link_debounce_interval_msec: The interface policy administrative port link debounce
                interval. Enables the debounce timer for physical interface ports and sets it
                for a specified amount of time in milliseconds. The debounce timer is disabled
                if you specify the time to 0 ms.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            physical_media_type: Values: ``auto``, ``sfp-10g-tx``. Default: ``auto``.
            speed: The interface policy administrative port speed. The data transfer rate for
                the port should match the destination to which the port is linked. The
                administrative speed can be changed only for certain ports, and not all speeds
                are available on all systems. Values: ``100G``, ``100M``, ``10G``, ``1G``,
                ``200G``, ``25G``, ``400G``, ``40G``, …. Default: ``inherit``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LinkLevelPolicyCursor",
            self._invoke_maker("link_level_policy", (name,), _prune(params)),
        )

    def mcp_policy(
        self,
        name: str,
        *,
        admin_state: NwIfAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        grace_period: str | None = None,
        grace_period_msec: str | None = None,
        maximum_number_of_vlans: str | None = None,
        mode: McpMcpMode | str | None = None,
        mcp_pdu_per_vlan: McpMcpPduPerVlan | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        init_delay_time: str | None = None,
        transmission_frequency: str | None = None,
        strict_tx_freq_msec: str | None = None,
        userdom: str | None = None,
    ) -> McpPolicyCursor:
        """Declare a ``mcpIfPol`` child under the infra level.

        Args:
            name: The name of the object.
            admin_state: The administrative state of the object or policy. Values: ``disabled``,
                ``enabled``. Default: ``enabled``.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            grace_period: For strict mode, grace period timeout in sec during which early loop
                detection takes place
            grace_period_msec: For strict mode, grace period timeout in millisec during which
                early loop detection takes place
            mode: Instance MCP mode Values: ``off``, ``on``. Default: ``off``.
            mcp_pdu_per_vlan: Values: ``off``, ``on``. Default: ``on``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            init_delay_time: For strict mode, delay time in seconds for mcp to wait before
                sending BPDUs. This gives time for STP on the external network to converge
            transmission_frequency: For strict mode, transmission frequency of MCP packets until
                grace period on each L2 interface in seconds
            strict_tx_freq_msec: For strict mode, transmission frequency of MCP packets until
                grace period on each L2 interface in milliseconds
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "McpPolicyCursor",
            self._invoke_maker("mcp_policy", (name,), _prune(params)),
        )

    def stp_policy(
        self,
        name: str,
        *,
        annotation: str | None = None,
        controls: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> StpPolicyCursor:
        """Declare a ``stpIfPol`` child under the infra level.

        The Spanning-Tree Protocol (STP) interface policy defines a common configuration that will
        apply to one or more interfaces. STP prevents loops from being formed when the interfaces
        are interconnected via multiple paths.

        Args:
            name: The STP interface policy name.
            annotation: User annotation. Suggested format orchestrator:value
            controls: Interface controls
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "StpPolicyCursor",
            self._invoke_maker("stp_policy", (name,), _prune(params)),
        )

    def storm_control_policy(
        self,
        name: str,
        *,
        annotation: str | None = None,
        broadcast_max_burst_size: int | None = None,
        bc_burst_rate: str | None = None,
        broadcast_traffic_rate: str | None = None,
        bc_rate_pps: int | None = None,
        max_burst_size: int | None = None,
        burst_rate: str | None = None,
        description: str | None = None,
        packet_type_uc_bc_mc_config_valid_yes_no: L1StormCtrlCfgValid | str | None = None,
        multicast_max_burst_size: int | None = None,
        mc_burst_rate: str | None = None,
        multicast_traffic_rate: str | None = None,
        mc_rate_pps: int | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        traffic_rate: str | None = None,
        tarffic_rate: int | None = None,
        storm_ctrl_action: L1StormCtrlActionT | str | None = None,
        storm_ctrl_soak_inst_count: str | None = None,
        packet_type: L1PktT | str | None = None,
        userdom: str | None = None,
        unknown_unicast_max_burst_size: int | None = None,
        uuc_burst_rate: str | None = None,
        unknown_unicast_traffic_rate: str | None = None,
        uuc_rate_pps: int | None = None,
    ) -> StormControlPolicyCursor:
        """Declare a ``stormctrlIfPol`` child under the infra level.

        The storm control interface policy. A traffic storm occurs when packets flood the LAN,
        creating excessive traffic and degrading network performance.

        Args:
            name: The storm control policy name.
            annotation: User annotation. Suggested format orchestrator:value
            broadcast_max_burst_size: burst rate in pps Default: ``0``.
            bc_burst_rate: burst rate in % (upto 1 decimal)
            broadcast_traffic_rate: rate in % (upto 1 decimal)
            bc_rate_pps: rate in pps Default: ``0``.
            max_burst_size: The packets per second (PPS) burst interval rate for the storm
                control policy. During this interval, the traffic level which is expressed as
                packets flowing per second through the port, is then compared with the traffic
                storm control level that you configured. Default: ``0``.
            burst_rate: The traffic burst rate percentage.
            description: Specifies a description of the policy definition.
            packet_type_uc_bc_mc_config_valid_yes_no: Values: ``Invalid``, ``Valid``. Default:
                ``Invalid``.
            multicast_max_burst_size: burst rate in pps Default: ``0``.
            mc_burst_rate: burst rate in % (upto 1 decimal)
            multicast_traffic_rate: rate in % (upto 1 decimal)
            mc_rate_pps: rate in pps Default: ``0``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            traffic_rate: The traffic rate percentage.
            tarffic_rate: The packets per second (PPS) interval rate for the storm control
                policy. During this interval, the traffic level which is expressed as packets
                flowing per second through the port, is then compared with the traffic storm
                control level that you configured. Default: ``0``.
            storm_ctrl_action: action drop/shutdown Values: ``drop``, ``shutdown``. Default:
                ``drop``.
            storm_ctrl_soak_inst_count: Instances required to declare port shutdown
            packet_type: The storm control interface policy type. The policy type prevents
                disruptions on ports by a broadcast, multicast, or unknown unicast traffic storm
                on physical interfaces. The policy type is set to ALL types of traffic by
                default and can not be changed. Values: ``all``, ``bcast``, ``mcast``, ``unk-
                ucast``. Default: ``all``.
            unknown_unicast_max_burst_size: burst rate in pps Default: ``0``.
            uuc_burst_rate: burst rate in % (upto 1 decimal)
            unknown_unicast_traffic_rate: rate in % (upto 1 decimal)
            uuc_rate_pps: rate in pps Default: ``0``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "StormControlPolicyCursor",
            self._invoke_maker("storm_control_policy", (name,), _prune(params)),
        )

    def vlan_pool(
        self,
        name: str,
        allocation_mode: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> VlanPoolCursor:
        """Declare a ``fvnsVlanInstP`` child under the infra level.

        The VLAN range namespace policy defines for ID ranges used for VLAN encapsulation.

        Args:
            name: The VLAN range namespace policy name.
            allocation_mode: The allocation mode of the VLAN pool.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
                "allocation_mode",
            )
        }
        return cast(
            "VlanPoolCursor",
            self._invoke_maker(
                "vlan_pool",
                (
                    name,
                    allocation_mode,
                ),
                _prune(params),
            ),
        )

    def aaep(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AaepCursor:
        """Declare a ``infraAttEntityP`` child under the infra level.

        The attached entity profile provides a template to deploy hypervisor policies on a large set
        of leaf ports. This also provides the association of a Virtual Machine Management (VMM)
        domain and the physical network infrastructure.

        Args:
            name: The attached entity profile name. This name can be up to 64 alphanumeric
                characters. Note that you cannot change this name after the object has been
                saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "AaepCursor",
            self._invoke_maker("aaep", (name,), _prune(params)),
        )

    def func_profile(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FuncProfileCursor:
        """Declare a ``infraFuncP`` child under the infra level.

        The hypervisor management function provides the policies used for hypervisor management and
        connectivity. For example, an endpoint group and encap VLAN.

        Args:
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "FuncProfileCursor",
            self._invoke_maker("func_profile", (), _prune(params)),
        )

    def access_port_profile(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AccessPortProfileCursor:
        """Declare a ``infraAccPortP`` child under the infra level.

        The interface profile enables you to specify the interface you want to configure.

        Args:
            name: The interface profile name. This name can be up to 64 alphanumeric characters.
                Note that you cannot change this name after the object has been saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "AccessPortProfileCursor",
            self._invoke_maker("access_port_profile", (name,), _prune(params)),
        )

    def leaf_profile(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LeafProfileCursor:
        """Declare a ``infraNodeP`` child under the infra level.

        The node profile enables you to specify which nodes (Example: a leaf) to configure.

        Args:
            name: The node policy name. This name can be up to 64 alphanumeric characters. Note
                that you cannot change this name after the object has been saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LeafProfileCursor",
            self._invoke_maker("leaf_profile", (name,), _prune(params)),
        )

    def spine_profile(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> SpineProfileCursor:
        """Declare a ``infraSpineP`` child under the infra level.

        Spine ProfileSpine Access Policy: It represents the template used for deploying node access
        configuration (ex. Configuration for connecting hypervisor, Fex, External network )

        Args:
            name: The name of the object.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "SpineProfileCursor",
            self._invoke_maker("spine_profile", (name,), _prune(params)),
        )


class InfraCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``infraInfra`` (infra level).

    Position: ``uni.infra``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> InfraCursor:
        """Set ``infraInfra`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class AaepCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``infraAttEntityP`` (aaep level).

    Position: ``uni.infra.aaep``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AaepCursor:
        """Set ``infraAttEntityP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        domain: str | None = None,
    ) -> AaepCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        domain: str | None = None,
    ) -> AaepCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class _AccessPortProfileMakers(Cursor):
    """Makers declared at the access_port_profile level (``infraAccPortP``)."""

    __slots__ = ()

    def port_selector(
        self,
        name: str,
        selector_type: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> PortSelectorCursor:
        """Declare a ``infraHPortS`` child under the access_port_profile level.

        The Host Port Selector is used for grouping ports between the node and the host (such as
        hypervisor).

        Args:
            name: The host port selector name. This name can be up to 64 alphanumeric
                characters. Note that you cannot change this name after the object has been
                saved.
            selector_type: The host port selector type.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
                "selector_type",
            )
        }
        return cast(
            "PortSelectorCursor",
            self._invoke_maker(
                "port_selector",
                (
                    name,
                    selector_type,
                ),
                _prune(params),
            ),
        )


class AccessPortProfileCursor(_AccessPortProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraAccPortP`` (access_port_profile level).

    Position: ``uni.infra.access_port_profile``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AccessPortProfileCursor:
        """Set ``infraAccPortP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class CdpPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``cdpIfPol`` (cdp_policy level).

    Position: ``uni.infra.cdp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        admin_state: NwIfAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> CdpPolicyCursor:
        """Set ``cdpIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _InfraDhcpRelayPolicyMakers(Cursor):
    """Makers declared at the dhcp_relay_policy level (``dhcpRelayP``)."""

    __slots__ = ()

    def provider(
        self,
        target_dn: str,
        *,
        dhcp_server_address: str | None = None,
        annotation: str | None = None,
        pref: str | None = None,
        userdom: str | None = None,
    ) -> InfraDhcpRelayPolicyProviderCursor:
        """Declare a ``dhcpRsProv`` child under the dhcp_relay_policy level.

        An endpoint group associated with the DHCP relay profile. Hosts/clients in the endpoint
        group can obtain IP addresses acquired via the DHCP relay profile.

        Args:
            target_dn: The distinguished name of the target endpoint group.
            dhcp_server_address: The DHCP server address. This address is configured onto the
                relationship between a relay profile and an endpoint group.
            annotation: User annotation. Suggested format orchestrator:value
            pref: DHCP server preferences
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "target_dn",
            )
        }
        return cast(
            "InfraDhcpRelayPolicyProviderCursor",
            self._invoke_maker("provider", (target_dn,), _prune(params)),
        )


class InfraDhcpRelayPolicyCursor(_InfraDhcpRelayPolicyMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``dhcpRelayP`` (dhcp_relay_policy level).

    Position: ``uni.infra.dhcp_relay_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        relay_mode: DhcpRelayVisibility | str | None = None,
        display_name: str | None = None,
        owner: DhcpOwner | str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> InfraDhcpRelayPolicyCursor:
        """Set ``dhcpRelayP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class InfraDppPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``qosDppPol`` (dpp_policy level).

    Position: ``uni.infra.dpp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        admin_st: QosDppPolAdminState | str | None = None,
        annotation: str | None = None,
        excessive_burst: str | None = None,
        excessive_burst_unit: DppBurstUnit | str | None = None,
        burst: str | None = None,
        burst_unit: DppBurstUnit | str | None = None,
        confirm_action: DppConformRateAction | str | None = None,
        conform_mark_cos: str | None = None,
        conform_mark_dscp: str | None = None,
        description: str | None = None,
        exceed_action: DppExceedRateAction | str | None = None,
        exceed_mark_cos: str | None = None,
        exceed_mark_dscp: str | None = None,
        bit_or_packet: DppMode | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        peak_rate: str | None = None,
        peak_rate_unit: DppRateUnit | str | None = None,
        rate: str | None = None,
        rate_unit: DppRateUnit | str | None = None,
        policer_sharing_mode: DppSharingMode | str | None = None,
        type: DppType | str | None = None,
        userdom: str | None = None,
        violate_action: DppViolateRateAction | str | None = None,
        violate_mark_cos: str | None = None,
        violate_mark_dscp: str | None = None,
    ) -> InfraDppPolicyCursor:
        """Set ``qosDppPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _FuncProfileMakers(Cursor):
    """Makers declared at the func_profile level (``infraFuncP``)."""

    __slots__ = ()

    def access_group(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AccessGroupCursor:
        """Declare a ``infraAccPortGrp`` child under the func_profile level.

        The interface policy group enables you to specify the interface policies you want to use.

        Args:
            name: The singular ports name. This name can be up to 64 alphanumeric characters.
                Note that you cannot change this name after the object has been saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "AccessGroupCursor",
            self._invoke_maker("access_group", (name,), _prune(params)),
        )

    def port_channel(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        link_aggregation_type: InfraLagT | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> PortChannelCursor:
        """Declare a ``infraAccBndlGrp`` child under the func_profile level.

        The bundle interface group enables you to specify the interface policy you want to use.

        Args:
            name: The bundled ports group name. This name can be up to 64 alphanumeric
                characters. Note that you cannot change this name after the object has been
                saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            link_aggregation_type: The bundled ports group link aggregation type: port channel
                vs virtual port channel. Values: ``fc-link``, ``link``, ``node``, ``not-
                aggregated``. Default: ``link``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "PortChannelCursor",
            self._invoke_maker("port_channel", (name,), _prune(params)),
        )


class FuncProfileCursor(_FuncProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraFuncP`` (func_profile level).

    Position: ``uni.infra.func_profile``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FuncProfileCursor:
        """Set ``infraFuncP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class LacpPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``lacpLagPol`` (lacp_policy level).

    Position: ``uni.infra.lacp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        control: PcIfControl | str | None = None,
        description: str | None = None,
        maximum_number_of_links: int | None = None,
        minimum_number_of_links: int | None = None,
        mode: LacpMode | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LacpPolicyCursor:
        """Set ``lacpLagPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _LeafProfileMakers(Cursor):
    """Makers declared at the leaf_profile level (``infraNodeP``)."""

    __slots__ = ()

    def leaf_selector(
        self,
        name: str,
        selector_type: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LeafSelectorCursor:
        """Declare a ``infraLeafS`` child under the leaf_profile level.

        The leaf selector enables you to select the interface to configure.

        Args:
            name: The leaf selector name
            selector_type: The leaf selector type
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
                "selector_type",
            )
        }
        return cast(
            "LeafSelectorCursor",
            self._invoke_maker(
                "leaf_selector",
                (
                    name,
                    selector_type,
                ),
                _prune(params),
            ),
        )


class LeafProfileCursor(_LeafProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraNodeP`` (leaf_profile level).

    Position: ``uni.infra.leaf_profile``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LeafProfileCursor:
        """Set ``infraNodeP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafProfileCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafProfileCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class LinkLevelPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``fabricHIfPol`` (link_level_policy level).

    Position: ``uni.infra.link_level_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        auto_negotiation_on_off: L1AutoNeg | str | None = None,
        description: str | None = None,
        dfe_delay_ms: str | None = None,
        enable_disable_emi_retrain: L1EmiRetrain | str | None = None,
        fec_mode: L1FECMode | str | None = None,
        link_debounce_interval_msec: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        physical_media_type: L1PortPhyMediaType | str | None = None,
        speed: L1Speed | str | None = None,
        userdom: str | None = None,
    ) -> LinkLevelPolicyCursor:
        """Set ``fabricHIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class LldpPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``lldpIfPol`` (lldp_policy level).

    Position: ``uni.infra.lldp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        receive_state: LldpAdminSt | str | None = None,
        transmit_state: LldpAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        dcbxp_version: LldpDCBXPVerType | str | None = None,
        userdom: str | None = None,
    ) -> LldpPolicyCursor:
        """Set ``lldpIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class McpPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``mcpIfPol`` (mcp_policy level).

    Position: ``uni.infra.mcp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        admin_state: NwIfAdminSt | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        grace_period: str | None = None,
        grace_period_msec: str | None = None,
        maximum_number_of_vlans: str | None = None,
        mode: McpMcpMode | str | None = None,
        mcp_pdu_per_vlan: McpMcpPduPerVlan | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        init_delay_time: str | None = None,
        transmission_frequency: str | None = None,
        strict_tx_freq_msec: str | None = None,
        userdom: str | None = None,
    ) -> McpPolicyCursor:
        """Set ``mcpIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _SpineProfileMakers(Cursor):
    """Makers declared at the spine_profile level (``infraSpineP``)."""

    __slots__ = ()

    def spine_selector(
        self,
        name: str,
        selector_type: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> SpineSelectorCursor:
        """Declare a ``infraSpineS`` child under the spine_profile level.

        Spine Selector

        Args:
            name: The name of the object.
            selector_type: The specific type of the object or component.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
                "selector_type",
            )
        }
        return cast(
            "SpineSelectorCursor",
            self._invoke_maker(
                "spine_selector",
                (
                    name,
                    selector_type,
                ),
                _prune(params),
            ),
        )


class SpineProfileCursor(_SpineProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraSpineP`` (spine_profile level).

    Position: ``uni.infra.spine_profile``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> SpineProfileCursor:
        """Set ``infraSpineP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class StormControlPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``stormctrlIfPol`` (storm_control_policy level).

    Position: ``uni.infra.storm_control_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        broadcast_max_burst_size: int | None = None,
        bc_burst_rate: str | None = None,
        broadcast_traffic_rate: str | None = None,
        bc_rate_pps: int | None = None,
        max_burst_size: int | None = None,
        burst_rate: str | None = None,
        description: str | None = None,
        packet_type_uc_bc_mc_config_valid_yes_no: L1StormCtrlCfgValid | str | None = None,
        multicast_max_burst_size: int | None = None,
        mc_burst_rate: str | None = None,
        multicast_traffic_rate: str | None = None,
        mc_rate_pps: int | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        traffic_rate: str | None = None,
        tarffic_rate: int | None = None,
        storm_ctrl_action: L1StormCtrlActionT | str | None = None,
        storm_ctrl_soak_inst_count: str | None = None,
        packet_type: L1PktT | str | None = None,
        userdom: str | None = None,
        unknown_unicast_max_burst_size: int | None = None,
        uuc_burst_rate: str | None = None,
        unknown_unicast_traffic_rate: str | None = None,
        uuc_rate_pps: int | None = None,
    ) -> StormControlPolicyCursor:
        """Set ``stormctrlIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class StpPolicyCursor(_InfraMakers, _UniMakers):
    """Typed cursor for ``stpIfPol`` (stp_policy level).

    Position: ``uni.infra.stp_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        controls: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> StpPolicyCursor:
        """Set ``stpIfPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _VlanPoolMakers(Cursor):
    """Makers declared at the vlan_pool level (``fvnsVlanInstP``)."""

    __slots__ = ()

    def range(
        self,
        from_: str,
        to: str,
        *,
        allocation_mode: FvnsBlkAllocMode | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        role: FvnsBlkRole | str | None = None,
        userdom: str | None = None,
    ) -> RangeCursor:
        """Declare a ``fvnsEncapBlk`` child under the vlan_pool level.

        The VLAN encapsulation block.

        Args:
            from_: Start of the encapsulation block.
            to: End of the encapsulation block.
            allocation_mode: Values: ``dynamic``, ``inherit``, ``static``. Default: ``inherit``.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies the description of a policy component.
            role: Role of the block. @@@ used only for domain in AVE mode @@@ external: On-the-
                wire encap. PVLAN/Access determined by the domain @@@ Internal: Encaps used
                internally Values: ``external``, ``internal``. Default: ``external``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "from_",
                "to",
            )
        }
        return cast(
            "RangeCursor",
            self._invoke_maker(
                "range",
                (
                    from_,
                    to,
                ),
                _prune(params),
            ),
        )


class VlanPoolCursor(_VlanPoolMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``fvnsVlanInstP`` (vlan_pool level).

    Position: ``uni.infra.vlan_pool``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> VlanPoolCursor:
        """Set ``fvnsVlanInstP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _PortSelectorMakers(Cursor):
    """Makers declared at the port_selector level (``infraHPortS``)."""

    __slots__ = ()

    def port_block(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_module_id: str | None = None,
        from_port_id: str | None = None,
        display_name: str | None = None,
        to_module_id: str | None = None,
        to_port_id: str | None = None,
        userdom: str | None = None,
    ) -> PortBlockCursor:
        """Declare a ``infraPortBlk`` child under the port_selector level.

        The port block enables you to specify a range of interfaces.

        Args:
            name: The port block name
            annotation: User annotation. Suggested format orchestrator:value
            description: The description of this configuration item.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "PortBlockCursor",
            self._invoke_maker("port_block", (name,), _prune(params)),
        )

    def sub_port_block(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_module_id: str | None = None,
        from_port_id: str | None = None,
        from_sub_port_id: str | None = None,
        display_name: str | None = None,
        to_module_id: str | None = None,
        to_port_id: str | None = None,
        to_sub_port_id: str | None = None,
        userdom: str | None = None,
    ) -> SubPortBlockCursor:
        """Declare a ``infraSubPortBlk`` child under the port_selector level.

        Access sub port (breakout) block

        Args:
            name: The name of the object.
            annotation: User annotation. Suggested format orchestrator:value
            description: The description of this configuration item.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "SubPortBlockCursor",
            self._invoke_maker("sub_port_block", (name,), _prune(params)),
        )


class PortSelectorCursor(_PortSelectorMakers, _AccessPortProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraHPortS`` (port_selector level).

    Position: ``uni.infra.access_port_profile.port_selector``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> PortSelectorCursor:
        """Set ``infraHPortS`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        policy_group: str | None = None,
    ) -> PortSelectorCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        policy_group: str | None = None,
    ) -> PortSelectorCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class InfraDhcpRelayPolicyProviderCursor(_InfraDhcpRelayPolicyMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``dhcpRsProv`` (provider level).

    Position: ``uni.infra.dhcp_relay_policy.provider``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        dhcp_server_address: str | None = None,
        annotation: str | None = None,
        pref: str | None = None,
        userdom: str | None = None,
    ) -> InfraDhcpRelayPolicyProviderCursor:
        """Set ``dhcpRsProv`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class AccessGroupCursor(_FuncProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraAccPortGrp`` (access_group level).

    Position: ``uni.infra.func_profile.access_group``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> AccessGroupCursor:
        """Set ``infraAccPortGrp`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        aaep: str | None = None,
        cdp: str | None = None,
        lldp: str | None = None,
        link_level: str | None = None,
        stp: str | None = None,
        mcp: str | None = None,
        storm_control: str | None = None,
    ) -> AccessGroupCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        aaep: str | None = None,
    ) -> AccessGroupCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class PortChannelCursor(_FuncProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraAccBndlGrp`` (port_channel level).

    Position: ``uni.infra.func_profile.port_channel``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        link_aggregation_type: InfraLagT | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> PortChannelCursor:
        """Set ``infraAccBndlGrp`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        aaep: str | None = None,
        cdp: str | None = None,
        lldp: str | None = None,
        lacp: str | None = None,
        link_level: str | None = None,
        stp: str | None = None,
        mcp: str | None = None,
        storm_control: str | None = None,
    ) -> PortChannelCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        aaep: str | None = None,
    ) -> PortChannelCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class _LeafSelectorMakers(Cursor):
    """Makers declared at the leaf_selector level (``infraLeafS``)."""

    __slots__ = ()

    def node_block(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_node_id: str | None = None,
        display_name: str | None = None,
        to_node_id: str | None = None,
        userdom: str | None = None,
    ) -> LeafSelectorNodeBlockCursor:
        """Declare a ``infraNodeBlk`` child under the leaf_selector level.

        A node block is a range of nodes. Each node block begins with the first port and ends with
        the last port.

        Args:
            name: Naming property — forms the object's RN.
            annotation: User annotation. Suggested format orchestrator:value
            description: The description of this configuration item.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LeafSelectorNodeBlockCursor",
            self._invoke_maker("node_block", (name,), _prune(params)),
        )


class LeafSelectorCursor(_LeafSelectorMakers, _LeafProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraLeafS`` (leaf_selector level).

    Position: ``uni.infra.leaf_profile.leaf_selector``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LeafSelectorCursor:
        """Set ``infraLeafS`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafSelectorCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafSelectorCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class _SpineSelectorMakers(Cursor):
    """Makers declared at the spine_selector level (``infraSpineS``)."""

    __slots__ = ()

    def node_block(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_node_id: str | None = None,
        display_name: str | None = None,
        to_node_id: str | None = None,
        userdom: str | None = None,
    ) -> SpineSelectorNodeBlockCursor:
        """Declare a ``infraNodeBlk`` child under the spine_selector level.

        A node block is a range of nodes. Each node block begins with the first port and ends with
        the last port.

        Args:
            name: Naming property — forms the object's RN.
            annotation: User annotation. Suggested format orchestrator:value
            description: The description of this configuration item.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "SpineSelectorNodeBlockCursor",
            self._invoke_maker("node_block", (name,), _prune(params)),
        )


class SpineSelectorCursor(_SpineSelectorMakers, _SpineProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraSpineS`` (spine_selector level).

    Position: ``uni.infra.spine_profile.spine_selector``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> SpineSelectorCursor:
        """Set ``infraSpineS`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class RangeCursor(_VlanPoolMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``fvnsEncapBlk`` (range level).

    Position: ``uni.infra.vlan_pool.range``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        allocation_mode: FvnsBlkAllocMode | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        role: FvnsBlkRole | str | None = None,
        userdom: str | None = None,
    ) -> RangeCursor:
        """Set ``fvnsEncapBlk`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class PortBlockCursor(_PortSelectorMakers, _AccessPortProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraPortBlk`` (port_block level).

    Position: ``uni.infra.access_port_profile.port_selector.port_block``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_module_id: str | None = None,
        from_port_id: str | None = None,
        display_name: str | None = None,
        to_module_id: str | None = None,
        to_port_id: str | None = None,
        userdom: str | None = None,
    ) -> PortBlockCursor:
        """Set ``infraPortBlk`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        policy_group: str | None = None,
    ) -> PortBlockCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        policy_group: str | None = None,
    ) -> PortBlockCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class SubPortBlockCursor(_PortSelectorMakers, _AccessPortProfileMakers, _InfraMakers, _UniMakers):
    """Typed cursor for ``infraSubPortBlk`` (sub_port_block level).

    Position: ``uni.infra.access_port_profile.port_selector.sub_port_block``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_module_id: str | None = None,
        from_port_id: str | None = None,
        from_sub_port_id: str | None = None,
        display_name: str | None = None,
        to_module_id: str | None = None,
        to_port_id: str | None = None,
        to_sub_port_id: str | None = None,
        userdom: str | None = None,
    ) -> SubPortBlockCursor:
        """Set ``infraSubPortBlk`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        policy_group: str | None = None,
    ) -> SubPortBlockCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        policy_group: str | None = None,
    ) -> SubPortBlockCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class LeafSelectorNodeBlockCursor(
    _LeafSelectorMakers, _LeafProfileMakers, _InfraMakers, _UniMakers
):
    """Typed cursor for ``infraNodeBlk`` (node_block level).

    Position: ``uni.infra.leaf_profile.leaf_selector.node_block``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_node_id: str | None = None,
        display_name: str | None = None,
        to_node_id: str | None = None,
        userdom: str | None = None,
    ) -> LeafSelectorNodeBlockCursor:
        """Set ``infraNodeBlk`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self

    def bind(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafSelectorNodeBlockCursor:
        """Declare lazy Rs references (resolved at push time)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind(self, **_prune(params))
        return self

    def bind_dn(
        self,
        *,
        interface_profile: str | None = None,
    ) -> LeafSelectorNodeBlockCursor:
        """Reference objects outside the design by raw DN."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.bind_dn(self, **_prune(params))
        return self


class SpineSelectorNodeBlockCursor(
    _SpineSelectorMakers, _SpineProfileMakers, _InfraMakers, _UniMakers
):
    """Typed cursor for ``infraNodeBlk`` (node_block level).

    Position: ``uni.infra.spine_profile.spine_selector.node_block``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        from_node_id: str | None = None,
        display_name: str | None = None,
        to_node_id: str | None = None,
        userdom: str | None = None,
    ) -> SpineSelectorNodeBlockCursor:
        """Set ``infraNodeBlk`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self
