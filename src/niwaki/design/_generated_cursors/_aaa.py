# ruff: noqa
# pyright: reportIncompatibleMethodOverride=false, reportUnnecessaryContains=false
# Generated by niwaki generate_design — do not edit manually.
# Re-generate: uv run python -m niwaki._codegen.generate_design
#
# The pyright/mypy override waivers are deliberate: typed cursors narrow the
# base Cursor signatures (set/bind take the curated kwargs), and same-label
# makers across ancestor mixins resolve by MRO — nearest level wins, exactly
# like the runtime.
#
# Typed cursor classes for the design DSL.  Signatures only — every body
# delegates to the dynamic runtime in niwaki.design._cursor.  Bodies read
# locals() at function entry (arguments only) and prune None values, so a
# parameter reaches the runtime only when the caller provided it.
"""Typed design cursors (generated) — aaa domain."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, cast

from niwaki.design._cursor import Cursor, _prune
from niwaki.design._node import Ref
from niwaki.design._generated_cursors._uni import _UniMakers

if TYPE_CHECKING:
    from niwaki.models._generated.enums.AaaAccess import AaaAccess
    from niwaki.models._generated.enums.AaaAccountStatus import AaaAccountStatus
    from niwaki.models._generated.enums.AaaBoolState import AaaBoolState
    from niwaki.models._generated.enums.AaaBoolean import AaaBoolean
    from niwaki.models._generated.enums.AaaClear import AaaClear
    from niwaki.models._generated.enums.AaaMonitorServerType import AaaMonitorServerType
    from niwaki.models._generated.enums.AaaOtpBoolean import AaaOtpBoolean
    from niwaki.models._generated.enums.AaaPwdClassFlags import AaaPwdClassFlags
    from niwaki.models._generated.enums.AaaPwdStrengthTestType import AaaPwdStrengthTestType
    from niwaki.models._generated.enums.AaaSNModeType import AaaSNModeType
    from niwaki.models._generated.enums.AaaauthenticationProtocol import AaaauthenticationProtocol
    from niwaki.models._generated.enums.ConditionSeverity import ConditionSeverity


class _AaaMakers(Cursor):
    """Makers declared at the aaa level (``aaaUserEp``)."""

    __slots__ = ()

    def radius(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        retries: int | None = None,
        timeout_in_seconds: int | None = None,
        userdom: str | None = None,
    ) -> RadiusCursor:
        """Declare a ``aaaRadiusEp`` child under the aaa level.

        The RADIUS endpoint policy is the global security management properties for RADIUS endpoints
        and RADIUS provider groups.

        Args:
            name: The name of the AAA definition object.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            retries: The number of attempts that the authentication method is tried. Default:
                ``1``.
            timeout_in_seconds: The amount of time between authentication attempts. Default:
                ``5``.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "RadiusCursor",
            self._invoke_maker("radius", (), _prune(params)),
        )

    def local_user(
        self,
        name: str,
        *,
        account_status: AaaAccountStatus | str | None = None,
        annotation: str | None = None,
        user_cert_attribute: str | None = None,
        clear_password_history: AaaClear | str | None = None,
        description: str | None = None,
        email_address: str | None = None,
        account_expiration_date: str | None = None,
        expiration_set_status: AaaBoolean | str | None = None,
        first_name: str | None = None,
        last_name: str | None = None,
        display_name: str | None = None,
        otp_enable_flag: AaaOtpBoolean | str | None = None,
        otp_key: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        phone_number: str | None = None,
        password: str | None = None,
        password_life_time: int | str | None = None,
        password_update_required: AaaBoolean | str | None = None,
        domain_read_write_rules_for_user: str | None = None,
        restricted_rbac_user: AaaBoolean | str | None = None,
        role_summary: str | None = None,
        unix_user_id: int | None = None,
        userdom: str | None = None,
    ) -> LocalUserCursor:
        """Declare a ``aaaUser`` child under the aaa level.

        A locally-authenticated user account.

        Args:
            name: The name of the locally-authenticated user.
            account_status: The status of the locally-authenticated user account. Values:
                ``active``, ``blocked``, ``inactive``. Default: ``active``.
            annotation: User annotation. Suggested format orchestrator:value
            clear_password_history: Allows the administrator to clear the password history of a
                locally-authenticated user. Values: ``no``, ``yes``. Default: ``no``.
            description: Specifies a description of the policy definition.
            email_address: The email address of the locally-authenticated user.
            account_expiration_date: The expiration date of the locally-authenticated user
                account. The expires property must be enabled to activate an expiration date.
            expiration_set_status: A property to enable an expiration date for the locally-
                authenticated user account. Values: ``no``, ``yes``. Default: ``no``.
            first_name: The first name of the locally-authenticated user.
            last_name: The last name of the locally-authenticated user.
            otp_enable_flag: Values: ``no``, ``yes``. Default: ``no``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            phone_number: The phone number of the locally-authenticated user.
            password: The system user password.
            password_life_time: The lifetime of the locally-authenticated user password.
                Default: ``0``.
            password_update_required: A boolean value indicating whether this account needs
                password update Values: ``no``, ``yes``. Default: ``no``.
            restricted_rbac_user: Values: ``no``, ``yes``. Default: ``no``.
            role_summary: current set of roles (keep explicit for audit log by PD and PM)
            unix_user_id: The UNIX identifier of the locally-authenticated user. Default: ``0``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LocalUserCursor",
            self._invoke_maker("local_user", (name,), _prune(params)),
        )

    def remote_user(
        self,
        name: str,
        *,
        annotation: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        role_summary: str | None = None,
        state_for_challenge_response: str | None = None,
        userdom: str | None = None,
    ) -> RemoteUserCursor:
        """Declare a ``aaaRemoteUser`` child under the aaa level.

        The remote user login account.

        Args:
            name: The name of the remote user.
            annotation: User annotation. Suggested format orchestrator:value
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            role_summary: current set of roles (keep explicit for audit log by PD and PM)
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "RemoteUserCursor",
            self._invoke_maker("remote_user", (name,), _prune(params)),
        )

    def aaa_role(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        privilege: frozenset[AaaAccess] | set[AaaAccess] | str | None = None,
        trigger_reset_to_factory_settings: AaaBoolean | str | None = None,
        userdom: str | None = None,
    ) -> AaaRoleCursor:
        """Declare a ``aaaRole`` child under the aaa level.

        An AAA role is a set of attributes and privileges that describe what a user is authorized to
        perform.

        Args:
            name: The name of the privilege role.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            privilege: The privilege(s) assigned to a role. Default: ``PydanticUndefined``.
            trigger_reset_to_factory_settings: Values: ``no``, ``yes``. Default: ``no``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "AaaRoleCursor",
            self._invoke_maker("aaa_role", (name,), _prune(params)),
        )

    def security_domain(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        restricted_rbac_domain: AaaBoolean | str | None = None,
        userdom: str | None = None,
    ) -> SecurityDomainCursor:
        """Declare a ``aaaDomain`` child under the aaa level.

        An AAA domain is the AAA security method for processing authentication requests.

        Args:
            name: The name of the AAA security domain. The name must begin with a letter and
                consist of up to 32 alphanumeric characters. Note that you cannot change this
                name after the object has been saved.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            restricted_rbac_domain: Values: ``no``, ``yes``. Default: ``no``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "SecurityDomainCursor",
            self._invoke_maker("security_domain", (name,), _prune(params)),
        )

    def login_domain(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LoginDomainCursor:
        """Declare a ``aaaLoginDomain`` child under the aaa level.

        An AAA login domain for authentication and authorization. The AAA configuration can be
        configured per domain.

        Args:
            name: The name of an AAA login domain.
            annotation: User annotation. Suggested format orchestrator:value
            description: The description for a login domain.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "LoginDomainCursor",
            self._invoke_maker("login_domain", (name,), _prune(params)),
        )

    def password_strength_policy(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        password_class_flags: frozenset[AaaPwdClassFlags]
        | set[AaaPwdClassFlags]
        | str
        | None = None,
        maximum_password_length: int | None = None,
        minimum_password_length: int | None = None,
        password_strength_test_type: AaaPwdStrengthTestType | str | None = None,
        userdom: str | None = None,
    ) -> PasswordStrengthPolicyCursor:
        """Declare a ``aaaPwdStrengthProfile`` child under the aaa level.

        Args:
            name: The name of the AAA definition object.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            password_class_flags: Default: ``PydanticUndefined``.
            maximum_password_length: Default: ``64``.
            minimum_password_length: Default: ``8``.
            password_strength_test_type: Values: ``custom``, ``default``. Default: ``default``.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "PasswordStrengthPolicyCursor",
            self._invoke_maker("password_strength_policy", (), _prune(params)),
        )

    def block_user_logins_policy(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        block_duration: int | None = None,
        description: str | None = None,
        enable_login_block: AaaBoolState | str | None = None,
        max_failed_attempts: int | None = None,
        max_failed_attempts_window: int | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> BlockUserLoginsPolicyCursor:
        """Declare a ``aaaBlockLoginProfile`` child under the aaa level.

        This MO stores config for blocking users that fail login continuously

        Args:
            name: The name of the AAA definition object.
            annotation: User annotation. Suggested format orchestrator:value
            block_duration: Duration in minutes for which future logins should be blocked
                Default: ``60``.
            description: Specifies a description of the policy definition.
            enable_login_block: Values: ``disable``, ``enable``. Default: ``disable``.
            max_failed_attempts: max failed login attempts before blocking user login Default:
                ``5``.
            max_failed_attempts_window: times in minutes for max login failures to occur before
                blocking the user Default: ``5``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "BlockUserLoginsPolicyCursor",
            self._invoke_maker("block_user_logins_policy", (), _prune(params)),
        )

    def pre_login_banner(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        application_banner_message: str | None = None,
        application_banner_severity: ConditionSeverity | str | None = None,
        description: str | None = None,
        login_gui_message: str | None = None,
        login_gui_string_message: str | None = None,
        use_text_base_pre_login_banner_message: bool | None = None,
        login_banner_message: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        show_application_banner: bool | None = None,
        switch_login_banner_message: str | None = None,
        userdom: str | None = None,
    ) -> PreLoginBannerCursor:
        """Declare a ``aaaPreLoginBanner`` child under the aaa level.

        A GUI banner is the informational banner to be displayed before user login authentication.

        Args:
            name: The name of the AAA definition object.
            annotation: User annotation. Suggested format orchestrator:value
            application_banner_severity: Values: ``cleared``, ``critical``, ``info``, ``major``,
                ``minor``, ``warning``. Default: ``info``.
            description: Specifies a description of the policy definition.
            login_gui_message: The contents of the GUI informational banner to be displayed
                before user login authentication. The GUI banner is a URL. Note that the URL
                site owner must allow the site to be placed in an iFrame to display the
                informational banner.
            use_text_base_pre_login_banner_message: Default: ``False``.
            login_banner_message: The contents of the CLI informational banner to be displayed
                before user login authentication. The CLI banner is a text based string printed
                as-is to the console.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            show_application_banner: Default: ``False``.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "PreLoginBannerCursor",
            self._invoke_maker("pre_login_banner", (), _prune(params)),
        )

    def service_node_cluster_settings(
        self,
        name: str,
        *,
        annotation: str | None = None,
        service_node_api_key: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        service_node_admin_password: str | None = None,
        service_node_kms_private_key: str | None = None,
        service_node_kms_public_key: str | None = None,
        service_node_mode: AaaSNModeType | str | None = None,
        userdom: str | None = None,
    ) -> ServiceNodeClusterSettingsCursor:
        """Declare a ``aaaServiceNodeCluster`` child under the aaa level.

        Args:
            name: The name of the object.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            service_node_admin_password: The system user password.
            service_node_mode: Values: ``ACI``, ``Standalone``. Default: ``ACI``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "ServiceNodeClusterSettingsCursor",
            self._invoke_maker("service_node_cluster_settings", (name,), _prune(params)),
        )

    def fabric_sec(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        fips_mode: AaaBoolState | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FabricSecCursor:
        """Declare a ``aaaFabricSec`` child under the aaa level.

        Args:
            name: The name of the AAA definition object.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            fips_mode: Values: ``disable``, ``enable``. Default: ``disable``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "FabricSecCursor",
            self._invoke_maker("fabric_sec", (), _prune(params)),
        )


class AaaCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaUserEp`` (aaa level).

    Position: ``uni.aaa``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        password_strength_check: AaaBoolean | str | None = None,
        userdom: str | None = None,
    ) -> AaaCursor:
        """Set ``aaaUserEp`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class AaaRoleCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaRole`` (aaa_role level).

    Position: ``uni.aaa.aaa_role``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        privilege: frozenset[AaaAccess] | set[AaaAccess] | str | None = None,
        trigger_reset_to_factory_settings: AaaBoolean | str | None = None,
        userdom: str | None = None,
    ) -> AaaRoleCursor:
        """Set ``aaaRole`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class BlockUserLoginsPolicyCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaBlockLoginProfile`` (block_user_logins_policy level).

    Position: ``uni.aaa.block_user_logins_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        block_duration: int | None = None,
        description: str | None = None,
        enable_login_block: AaaBoolState | str | None = None,
        max_failed_attempts: int | None = None,
        max_failed_attempts_window: int | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> BlockUserLoginsPolicyCursor:
        """Set ``aaaBlockLoginProfile`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class FabricSecCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaFabricSec`` (fabric_sec level).

    Position: ``uni.aaa.fabric_sec``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        fips_mode: AaaBoolState | str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FabricSecCursor:
        """Set ``aaaFabricSec`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class LocalUserCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaUser`` (local_user level).

    Position: ``uni.aaa.local_user``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        account_status: AaaAccountStatus | str | None = None,
        annotation: str | None = None,
        user_cert_attribute: str | None = None,
        clear_password_history: AaaClear | str | None = None,
        description: str | None = None,
        email_address: str | None = None,
        account_expiration_date: str | None = None,
        expiration_set_status: AaaBoolean | str | None = None,
        first_name: str | None = None,
        last_name: str | None = None,
        display_name: str | None = None,
        otp_enable_flag: AaaOtpBoolean | str | None = None,
        otp_key: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        phone_number: str | None = None,
        password: str | None = None,
        password_life_time: int | str | None = None,
        password_update_required: AaaBoolean | str | None = None,
        domain_read_write_rules_for_user: str | None = None,
        restricted_rbac_user: AaaBoolean | str | None = None,
        role_summary: str | None = None,
        unix_user_id: int | None = None,
        userdom: str | None = None,
    ) -> LocalUserCursor:
        """Set ``aaaUser`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class LoginDomainCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaLoginDomain`` (login_domain level).

    Position: ``uni.aaa.login_domain``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> LoginDomainCursor:
        """Set ``aaaLoginDomain`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class PasswordStrengthPolicyCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaPwdStrengthProfile`` (password_strength_policy level).

    Position: ``uni.aaa.password_strength_policy``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        password_class_flags: frozenset[AaaPwdClassFlags]
        | set[AaaPwdClassFlags]
        | str
        | None = None,
        maximum_password_length: int | None = None,
        minimum_password_length: int | None = None,
        password_strength_test_type: AaaPwdStrengthTestType | str | None = None,
        userdom: str | None = None,
    ) -> PasswordStrengthPolicyCursor:
        """Set ``aaaPwdStrengthProfile`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class PreLoginBannerCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaPreLoginBanner`` (pre_login_banner level).

    Position: ``uni.aaa.pre_login_banner``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        application_banner_message: str | None = None,
        application_banner_severity: ConditionSeverity | str | None = None,
        description: str | None = None,
        login_gui_message: str | None = None,
        login_gui_string_message: str | None = None,
        use_text_base_pre_login_banner_message: bool | None = None,
        login_banner_message: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        show_application_banner: bool | None = None,
        switch_login_banner_message: str | None = None,
        userdom: str | None = None,
    ) -> PreLoginBannerCursor:
        """Set ``aaaPreLoginBanner`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _RadiusMakers(Cursor):
    """Makers declared at the radius level (``aaaRadiusEp``)."""

    __slots__ = ()

    def radius_provider(
        self,
        name: str,
        *,
        annotation: str | None = None,
        port: int | None = None,
        authentication_protocol: AaaauthenticationProtocol | str | None = None,
        description: str | None = None,
        key: str | None = None,
        periodic_server_monitoring: AaaMonitorServerType | str | None = None,
        periodic_server_monitoring_password: str | None = None,
        periodic_server_monitoring_username: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        retries: int | None = None,
        timeout_in_seconds: int | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderCursor:
        """Declare a ``aaaRadiusProvider`` child under the radius level.

        A RADIUS provider is a remote server supporting the RADIUS protocol that will be used for
        authentication.

        Args:
            name: The name of the RADIUS service provider.
            annotation: User annotation. Suggested format orchestrator:value
            port: The service port number for the RADIUS service. Default: ``1812``.
            authentication_protocol: The RADIUS authentication protocol. Values: ``chap``,
                ``mschap``, ``pap``. Default: ``pap``.
            description: Specifies a description of the policy definition.
            key: A password for the AAA provider database.
            periodic_server_monitoring: Values: ``disabled``, ``enabled``. Default:
                ``disabled``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
            retries: Default: ``1``.
            timeout_in_seconds: The timeout for communication with a RADIUS provider server.
                Default: ``5``.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "RadiusProviderCursor",
            self._invoke_maker("radius_provider", (name,), _prune(params)),
        )

    def radius_provider_group(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderGroupCursor:
        """Declare a ``aaaRadiusProviderGroup`` child under the radius level.

        A RADIUS provider group is a group of remote servers supporting the RADIUS protocol for
        authentication.

        Args:
            name: The name of the RADIUS provider group.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "RadiusProviderGroupCursor",
            self._invoke_maker("radius_provider_group", (name,), _prune(params)),
        )


class RadiusCursor(_RadiusMakers, _AaaMakers, _UniMakers):
    """Typed cursor for ``aaaRadiusEp`` (radius level).

    Position: ``uni.aaa.radius``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        name: str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        retries: int | None = None,
        timeout_in_seconds: int | None = None,
        userdom: str | None = None,
    ) -> RadiusCursor:
        """Set ``aaaRadiusEp`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class RemoteUserCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaRemoteUser`` (remote_user level).

    Position: ``uni.aaa.remote_user``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        role_summary: str | None = None,
        state_for_challenge_response: str | None = None,
        userdom: str | None = None,
    ) -> RemoteUserCursor:
        """Set ``aaaRemoteUser`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class SecurityDomainCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaDomain`` (security_domain level).

    Position: ``uni.aaa.security_domain``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        restricted_rbac_domain: AaaBoolean | str | None = None,
        userdom: str | None = None,
    ) -> SecurityDomainCursor:
        """Set ``aaaDomain`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class ServiceNodeClusterSettingsCursor(_AaaMakers, _UniMakers):
    """Typed cursor for ``aaaServiceNodeCluster`` (service_node_cluster_settings level).

    Position: ``uni.aaa.service_node_cluster_settings``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        service_node_api_key: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        service_node_admin_password: str | None = None,
        service_node_kms_private_key: str | None = None,
        service_node_kms_public_key: str | None = None,
        service_node_mode: AaaSNModeType | str | None = None,
        userdom: str | None = None,
    ) -> ServiceNodeClusterSettingsCursor:
        """Set ``aaaServiceNodeCluster`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class RadiusProviderCursor(_RadiusMakers, _AaaMakers, _UniMakers):
    """Typed cursor for ``aaaRadiusProvider`` (radius_provider level).

    Position: ``uni.aaa.radius.radius_provider``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        port: int | None = None,
        authentication_protocol: AaaauthenticationProtocol | str | None = None,
        description: str | None = None,
        key: str | None = None,
        periodic_server_monitoring: AaaMonitorServerType | str | None = None,
        periodic_server_monitoring_password: str | None = None,
        periodic_server_monitoring_username: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        retries: int | None = None,
        timeout_in_seconds: int | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderCursor:
        """Set ``aaaRadiusProvider`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _RadiusProviderGroupMakers(Cursor):
    """Makers declared at the radius_provider_group level (``aaaRadiusProviderGroup``)."""

    __slots__ = ()

    def provider(
        self,
        name: str,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        order_in_which_providers_are_tried: int | str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderGroupProviderCursor:
        """Declare a ``aaaProviderRef`` child under the radius_provider_group level.

        A member of an AAA provider group.

        Args:
            name: The name of an existing AAA service provider.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            order_in_which_providers_are_tried: The relative priority in which the AAA provider
                will be contacted within the provider group. Default: ``0``.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "name",
            )
        }
        return cast(
            "RadiusProviderGroupProviderCursor",
            self._invoke_maker("provider", (name,), _prune(params)),
        )


class RadiusProviderGroupCursor(_RadiusProviderGroupMakers, _RadiusMakers, _AaaMakers, _UniMakers):
    """Typed cursor for ``aaaRadiusProviderGroup`` (radius_provider_group level).

    Position: ``uni.aaa.radius.radius_provider_group``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderGroupCursor:
        """Set ``aaaRadiusProviderGroup`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class RadiusProviderGroupProviderCursor(
    _RadiusProviderGroupMakers, _RadiusMakers, _AaaMakers, _UniMakers
):
    """Typed cursor for ``aaaProviderRef`` (provider level).

    Position: ``uni.aaa.radius.radius_provider_group.provider``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        display_name: str | None = None,
        order_in_which_providers_are_tried: int | str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> RadiusProviderGroupProviderCursor:
        """Set ``aaaProviderRef`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self
