# ruff: noqa
# pyright: reportIncompatibleMethodOverride=false, reportUnnecessaryContains=false
# Generated by niwaki generate_design — do not edit manually.
# Re-generate: uv run python -m niwaki._codegen.generate_design
#
# The pyright/mypy override waivers are deliberate: typed cursors narrow the
# base Cursor signatures (set/bind take the curated kwargs), and same-label
# makers across ancestor mixins resolve by MRO — nearest level wins, exactly
# like the runtime.
#
# Typed cursor classes for the design DSL.  Signatures only — every body
# delegates to the dynamic runtime in niwaki.design._cursor.  Bodies read
# locals() at function entry (arguments only) and prune None values, so a
# parameter reaches the runtime only when the caller provided it.
"""Typed design cursors (generated) — controller domain."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, cast

from niwaki.design._cursor import Cursor, _prune
from niwaki.design._node import Ref
from niwaki.design._generated_cursors._uni import _UniMakers

if TYPE_CHECKING:
    from niwaki.models._generated.enums.TopNodeRoleP import TopNodeRoleP
    from niwaki.models._generated.enums.TopNodeType import TopNodeType


class _ControllerMakers(Cursor):
    """Makers declared at the controller level (``ctrlrInst``)."""

    __slots__ = ()

    def fabric_membership(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FabricMembershipCursor:
        """Declare a ``fabricNodeIdentPol`` child under the controller level.

        A container for the node identity profile.

        Args:
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies a description of the policy definition.
            owner_key: The key for enabling clients to own their data for entity correlation.
            owner_tag: A tag for enabling clients to add their own data. For example, to
                indicate who created this object.
        """
        params = {k: v for k, v in locals().items() if k not in ("self",)}
        return cast(
            "FabricMembershipCursor",
            self._invoke_maker("fabric_membership", (), _prune(params)),
        )


class ControllerCursor(_ControllerMakers, _UniMakers):
    """Typed cursor for ``ctrlrInst`` (controller level).

    Position: ``uni.controller``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> ControllerCursor:
        """Set ``ctrlrInst`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class _FabricMembershipMakers(Cursor):
    """Makers declared at the fabric_membership level (``fabricNodeIdentPol``)."""

    __slots__ = ()

    def fabric_node_member(
        self,
        serial: str,
        *,
        ext_pool_id: str | None = None,
        fabric_id: str | None = None,
        id: str | None = None,
        node_type: TopNodeType | str | None = None,
        pod_id: str | None = None,
        role: TopNodeRoleP | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        userdom: str | None = None,
    ) -> FabricNodeMemberCursor:
        """Declare a ``fabricNodeIdentP`` child under the fabric_membership level.

        The node identity profile assigns IDs to the fabric nodes.

        Args:
            serial: The node manufacturer serial number.
            node_type: Values: ``remote-leaf-wan``, ``tier-2-leaf``, ``unspecified``,
                ``virtual``. Default: ``unspecified``.
            role: The system role type. Values: ``leaf``, ``spine``, ``unspecified``. Default:
                ``unspecified``.
            annotation: User annotation. Suggested format orchestrator:value
            description: Specifies the description of a policy component.
        """
        params = {
            k: v
            for k, v in locals().items()
            if k
            not in (
                "self",
                "serial",
            )
        }
        return cast(
            "FabricNodeMemberCursor",
            self._invoke_maker("fabric_node_member", (serial,), _prune(params)),
        )


class FabricMembershipCursor(_FabricMembershipMakers, _ControllerMakers, _UniMakers):
    """Typed cursor for ``fabricNodeIdentPol`` (fabric_membership level).

    Position: ``uni.controller.fabric_membership``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        owner_key: str | None = None,
        owner_tag: str | None = None,
        userdom: str | None = None,
    ) -> FabricMembershipCursor:
        """Set ``fabricNodeIdentPol`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self


class FabricNodeMemberCursor(_FabricMembershipMakers, _ControllerMakers, _UniMakers):
    """Typed cursor for ``fabricNodeIdentP`` (fabric_node_member level).

    Position: ``uni.controller.fabric_membership.fabric_node_member``

    Ancestor makers (implicit pop) come from the inherited mixins,
    nearest level first — the MRO mirrors the runtime resolution.
    """

    __slots__ = ()

    def set(
        self,
        *,
        ext_pool_id: str | None = None,
        fabric_id: str | None = None,
        id: str | None = None,
        node_type: TopNodeType | str | None = None,
        pod_id: str | None = None,
        role: TopNodeRoleP | str | None = None,
        annotation: str | None = None,
        description: str | None = None,
        name: str | None = None,
        display_name: str | None = None,
        userdom: str | None = None,
    ) -> FabricNodeMemberCursor:
        """Set ``fabricNodeIdentP`` attributes (merged; validated eagerly)."""
        params = {k: v for k, v in locals().items() if k != "self"}
        Cursor.set(self, **_prune(params))
        return self
