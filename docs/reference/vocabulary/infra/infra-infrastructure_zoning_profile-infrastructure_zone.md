<!--
Generated by niwaki generate_docs — do not edit manually.
Re-generate: uv run python -m niwaki._codegen.generate_docs
-->

(vocab-infra-infrastructure_zoning_profile-infrastructure_zone)=

# `infra.infrastructure_zoning_profile.infrastructure_zone`

Infrastructure Zone: Represents a policy deployment zone in the fabric. A zone consists of member nodes

| | |
| --- | --- |
| ACI class | `infrazoneZone` |
| RN | `zone-{name}` |
| Cursor | `InfrastructureZoneCursor` |
| Parent | {ref}`infra.infrastructure_zoning_profile <vocab-infra-infrastructure_zoning_profile>` |
| Atomic | — |

## Attributes

Accepted as keyword arguments of `.infrastructure_zone(name)` and of `.set(**attrs)` on this cursor.  Enum parameters also accept the plain string.

| parameter | wire | type | values | default | description |
| --- | --- | --- | --- | --- | --- |
| `name` *(positional)* | `name` | `str` | — | — | — |
| `depl_mode` | `deplMode` | {ref}`InfrazoneDeplMode <enum-infrazonedeplmode>` | `disabled`, `enabled`, `triggered` | `enabled` | Deployment Mode: Used for scheduling policy push to the member nodes of the zone. 1. enabled: Once set, APIC will push pending changes to the member nodes of the zone. Any change after it will be immediately pushed to the member nodes. 2. |
| `description` | `descr` | `str` | — | — | Specifies the description of a policy component. |

## Children

| maker | creates | position |
| --- | --- | --- |
| `.node_group(name, **attrs)` | `infrazoneNodeGrp` | {ref}`infra.infrastructure_zoning_profile.infrastructure_zone.node_group <vocab-infra-infrastructure_zoning_profile-infrastructure_zone-node_group>` |
| `.pod_group(name, **attrs)` | `infrazonePodGrp` | {ref}`infra.infrastructure_zoning_profile.infrastructure_zone.pod_group <vocab-infra-infrastructure_zoning_profile-infrastructure_zone-pod_group>` |

## APIC diagnostics

States the APIC can flag on this class — it accepts the configuration and reports the inconsistency afterwards.

**Faults**

| code | condition |
| --- | --- |
| `F3872` | `fltInfrazoneZoneConfigZoneDisabled` |
