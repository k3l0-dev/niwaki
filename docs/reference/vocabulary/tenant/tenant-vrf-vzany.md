<!--
Generated by niwaki generate_docs — do not edit manually.
Re-generate: uv run python -m niwaki._codegen.generate_docs
-->

(vocab-tenant-vrf-vzany)=

# `tenant.vrf.vzany`

vzAny associates all endpoint groups (EPGs) in a context (fvCtx) to one or more contracts (vzBrCP), rather than creating a separate contract relation for each EPG. EPGs can only communicate with other EPGs according to contract rules.

| | |
| --- | --- |
| ACI class | `vzAny` |
| RN | `any` |
| Cursor | `VzanyCursor` |
| Parent | {ref}`tenant.vrf <vocab-tenant-vrf>` |
| Atomic | — |

## Attributes

Accepted as keyword arguments of `.vzany()` and of `.set(**attrs)` on this cursor.  Enum parameters also accept the plain string.

| parameter | wire | type | values | default | description |
| --- | --- | --- | --- | --- | --- |
| `description` | `descr` | `str` | — | — | Specifies the description of a policy component. |
| `match_type` | `matchT` | {ref}`VzMatchT <enum-vzmatcht>` | `All`, `AtleastOne`, `AtmostOne`, `None` | `AtleastOne` | Represents the provider label match criteria. |
| `name` | `name` | `str` | — | — | — |
| `preferred_group_member` | `prefGrMemb` | {ref}`VzPrefGrMemb <enum-vzprefgrmemb>` | `disabled`, `enabled` | `disabled` | Represents parameter used to determine if EPgs can be divided in a the context can be divided in two subgroups. |

## Children

| maker | creates | position |
| --- | --- | --- |
| `.provider_label(name, **attrs)` | `vzProvLbl` | {ref}`tenant.vrf.vzany.provider_label <vocab-tenant-vrf-vzany-provider_label>` |
| `.consumer_label(name, **attrs)` | `vzConsLbl` | {ref}`tenant.vrf.vzany.consumer_label <vocab-tenant-vrf-vzany-consumer_label>` |
| `.provider_subject_label(name, **attrs)` | `vzProvSubjLbl` | {ref}`tenant.vrf.vzany.provider_subject_label <vocab-tenant-vrf-vzany-provider_subject_label>` |
| `.consumer_subject_label(name, **attrs)` | `vzConsSubjLbl` | {ref}`tenant.vrf.vzany.consumer_subject_label <vocab-tenant-vrf-vzany-consumer_subject_label>` |
| `.provider_contract_label(name, **attrs)` | `vzProvCtrctLbl` | {ref}`tenant.vrf.vzany.provider_contract_label <vocab-tenant-vrf-vzany-provider_contract_label>` |
| `.consumer_contract_label(name, **attrs)` | `vzConsCtrctLbl` | {ref}`tenant.vrf.vzany.consumer_contract_label <vocab-tenant-vrf-vzany-consumer_contract_label>` |

## Bind aliases

Lazy references, resolved closed-world at push time.

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `imported_contract=` | `vzCPIf` | name | `vzRsAnyToConsIf` |

## Verbs

- `.provide(name)` → `vzRsAnyToProv` targeting `vzBrCP`
- `.consume(name)` → `vzRsAnyToCons` targeting `vzBrCP`

## APIC diagnostics

States the APIC can flag on this class — it accepts the configuration and reports the inconsistency afterwards.

**Faults**

| code | condition |
| --- | --- |
| `F0418` | `fltVzAnyConfigurationFailed` |
