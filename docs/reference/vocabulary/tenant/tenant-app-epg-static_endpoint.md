<!--
Generated by niwaki generate_docs — do not edit manually.
Re-generate: uv run python -m niwaki._codegen.generate_docs
-->

(vocab-tenant-app-epg-static_endpoint)=

# `tenant.app.epg.static_endpoint`

The static endpoint represents a silent client attached to the fabric which will not produce traffic of its own. The configuration of a static endpoint will prevent the client from aging out of relevant addressing tables.

| | |
| --- | --- |
| ACI class | `fvStCEp` |
| RN | `stcep-{macaddress}-type-{type}` |
| Cursor | `StaticEndpointCursor` |
| Parent | {ref}`tenant.app.epg <vocab-tenant-app-epg>` |
| Atomic | — |

## Attributes

Accepted as keyword arguments of `.static_endpoint(macaddress, type)` and of `.set(**attrs)` on this cursor.  Enum parameters also accept the plain string.

| parameter | wire | type | values | default | description |
| --- | --- | --- | --- | --- | --- |
| `macaddress` *(positional)* | `mac` | `str` | — | — | The MAC address of the endpoint. |
| `type` *(positional)* | `type` | {ref}`FvStCEpType <enum-fvstceptype>` | `silent-host`, `tep`, `vep` | — | The specific type of endpoint. |
| `encap` | `encap` | `str` | — | — | The encapsulation (VXLAN or VLAN) to be used for this endpoint. |
| `id` | `id` | `int` | — | `0` | An identifier . |
| `ip_address` | `ip` | `str` | — | — | The IP address of the endpoint. |
| `name` | `name` | `str` | — | — | The name of the object. |

## Children

| maker | creates | position |
| --- | --- | --- |
| `.static_ip(static_ep_ip_address, **attrs)` | `fvStIp` | {ref}`tenant.app.epg.static_endpoint.static_ip <vocab-tenant-app-epg-static_endpoint-static_ip>` |

## Bind aliases

Lazy references, resolved closed-world at push time.

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `path=` | `fabricAPathEp` | DN — `bind_dn()` allowed | `fvRsStCEpToPathEp` |
| `fabric_node=` | `fabricNode` | DN — `bind_dn()` allowed | `fvRsStCEpToNode` |

## APIC diagnostics

States the APIC can flag on this class — it accepts the configuration and reports the inconsistency afterwards.

**Config issues**

| code | meaning |
| --- | --- |
| `multiple-tag-selector-match` | Tag(s) matching more than one ESG TagSelector. Endpoints can only be associated to a single ESG TagSelector via Tags. |
| `multiple-tag-selector-match-after-single-match` | Tag(s) are now matching more than one ESG TagSelector. The original match is still active. Endpoints can only be associated to a single ESG TagSelector via Tags. |
| `path-not-present` | Path Validations |

**Faults**

| code | condition |
| --- | --- |
| `F0524` | `fltFvStCEpConfigurationFailed` |
| `F4133` | `fltFvStCEpConfigurationError` |
