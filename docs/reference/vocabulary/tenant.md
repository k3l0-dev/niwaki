<!--
Generated by niwaki generate_docs — do not edit manually.
Re-generate: uv run python -m niwaki._codegen.generate_docs
-->

# tenant — logical networking

VRFs, bridge domains, application profiles, EPGs, contracts and filters — one `uni/tn-*` subtree per tenant.  Root factory: `tenant(name)`.

## `tenant`

ACI class `fvTenant` — RN `tn-{name}` — cursor `TenantCursor`.

**Makers**

- `.app(name, **attrs)` → `fvAp`
- `.bd(name, **attrs)` → `fvBD`
- `.vrf(name, **attrs)` → `fvCtx`
- `.l3out(name, **attrs)` → `l3extOut`
- `.filter(name, **attrs)` → `vzFilter`
- `.contract(name, **attrs)` → `vzBrCP`

## `tenant.app`

ACI class `fvAp` — RN `ap-{name}` — cursor `AppCursor`.

**Makers**

- `.epg(name, **attrs)` → `fvAEPg`

## `tenant.app.epg`

ACI class `fvAEPg` — RN `epg-{name}` — cursor `EpgCursor`.

**Makers**

- `.static_path(target_dn, **attrs)` → `fvRsPathAtt`

**Bind aliases** (lazy references, resolved closed-world at push time)

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `bd=` | `fvBD` | name | `fvRsBd` |
| `domain=` | `infraDomP` *(abstract: `extdevGroupP`, `fcDomP`, `l2extDomP`, `l3extDomP`…)* | DN — `bind_dn()` allowed | `fvRsDomAtt` |

**Verbs**

- `.provide(name)` → `fvRsProv` targeting `vzBrCP`
- `.consume(name)` → `fvRsCons` targeting `vzBrCP`

## `tenant.app.epg.static_path`

ACI class `fvRsPathAtt` — RN `rspathAtt-[{target_dn}]` — cursor `StaticPathCursor`.

## `tenant.bd`

ACI class `fvBD` — RN `BD-{name}` — cursor `BdCursor`.

**Makers**

- `.subnet(subnet, **attrs)` → `fvSubnet`

**Bind aliases** (lazy references, resolved closed-world at push time)

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `vrf=` | `fvCtx` | name | `fvRsCtx` |
| `l3out=` | `l3extOut` | name | `fvRsBDToOut` |

## `tenant.bd.subnet`

ACI class `fvSubnet` — RN `subnet-[{subnet}]` — cursor `SubnetCursor`.

## `tenant.vrf`

ACI class `fvCtx` — RN `ctx-{name}` — cursor `VrfCursor`.

**Makers**

- `.pim(**attrs)` → `pimCtxP`

**Bind aliases** (lazy references, resolved closed-world at push time)

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `l3out=` | `l3extOut` | name | `l3extRsEctx` (on the target side) |

## `tenant.vrf.pim`

ACI class `pimCtxP` — RN `pimctxp` — cursor `PimCursor`.

## `tenant.l3out`

ACI class `l3extOut` — RN `out-{name}` — cursor `L3outCursor`.

**Bind aliases** (lazy references, resolved closed-world at push time)

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `vrf=` | `fvCtx` | name | `l3extRsEctx` |

## `tenant.filter`

ACI class `vzFilter` — RN `flt-{name}` — cursor `FilterCursor`.

**Makers**

- `.entry(name, tcp, udp, **attrs)` → `vzEntry`

## `tenant.filter.entry`

ACI class `vzEntry` — RN `e-{name}` — cursor `EntryCursor`.

**Sugar parameters**

- `tcp=` — expanded to the real schema fields at the call site
- `udp=` — expanded to the real schema fields at the call site

## `tenant.contract`

ACI class `vzBrCP` — RN `brc-{name}` — cursor `ContractCursor`.

**Makers**

- `.subject(name, **attrs)` → `vzSubj`

## `tenant.contract.subject`

ACI class `vzSubj` — RN `subj-{name}` — cursor `SubjectCursor`.

**Bind aliases** (lazy references, resolved closed-world at push time)

| alias | target | flavor | relation |
| --- | --- | --- | --- |
| `filter=` | `vzFilter` | name | `vzRsSubjFiltAtt` |
